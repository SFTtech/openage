// Copyright 2014-2014 the openage authors. See copying.md for legal info.

#ifndef OPENAGE_CONFIG_H_
#define OPENAGE_CONFIG_H_

namespace openage {
namespace config {

// anonymous namespace to provide local linkage
namespace {

constexpr const char *global_asset_dir = "${INSTALLED_GLOBAL_ASSET_DIR}";
constexpr const char *version = "${PROJECT_VERSION}";
constexpr const char *config_option_string = "${CONFIG_OPTION_STRING}";

}

#define HAS_INOTIFY ${HAS_INOTIFY}
#define HAS_GPERFTOOLS_PROFILER ${HAS_GPERFTOOLS_PROFILER}
#define HAS_GPERFTOOLS_TCMALLOC ${HAS_GPERFTOOLS_TCMALLOC}

} // namespace config
} // namespace openage

#endif
