// Copyright 2014-2016 the openage authors. See copying.md for legal info.

// ${AUTOGEN_WARNING}

#include <libopenage/config.h>

namespace openage {
namespace config {

const char *const global_asset_dir = "${INSTALLED_GLOBAL_ASSET_DIR}";
const char *const version = "${PROJECT_VERSION}";
const char *const config_option_string = "${CONFIG_OPTION_STRING}";

}} // openage::config
