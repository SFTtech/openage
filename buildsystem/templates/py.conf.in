[project]
version=${PROJECT_VERSION}

[build]
source_dir=${CMAKE_SOURCE_DIR}
binary_dir=${CMAKE_BINARY_DIR}

[cython]
cc=${CMAKE_C_COMPILER}
cxx=${CMAKE_CXX_COMPILER}
cxxflags=${CYTHON_CXX_FLAGS}
ldflags=${CMAKE_SHARED_LINKER_FLAGS}
inplaceldflags=${CYTHON_INPLACE_LINKER_FLAGS}
pxd_dir=${PXDGEN_DIR}
